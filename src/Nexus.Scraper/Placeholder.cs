namespace Nexus.Scraper;

/// <summary>
/// Placeholder type. Real implementation of
/// <see cref="Nexus.Core.Interfaces.IStoryblocksScraper"/> is BLOCKED on the
/// human providing the Storyblocks CSS selectors (see CLAUDE.md §8 and the
/// HUMAN INPUT REQUIRED checkpoint in the README).
/// </summary>
internal static class ScaffoldMarker
{
    public const string Name = nameof(Nexus.Scraper);
}
